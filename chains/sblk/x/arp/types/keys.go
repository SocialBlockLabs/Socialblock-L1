package types

const (
	// ModuleName defines the module name
	ModuleName = "arp"

	// StoreKey is the primary module store key
	StoreKey = ModuleName

	// RouterKey is the message route for the module
	RouterKey = ModuleName

	// ParamsKey is the key for storing Params in KV store
	ParamsKey = "params"
)

